# ESP-IDF partition table — justicetattoo, 4 MB flash.
# Replaces the built-in SINGLE_APP_LARGE table: the firmware shrinks the app
# partition to make room for a dedicated "movie" partition that holds the
# wirelessly-uploaded JTM1 blob (see PLAN.md §4). The "undefined" data subtype
# is generic — the firmware locates this partition at runtime by its label.
#
# Name,     Type, SubType,   Offset,   Size
nvs,        data, nvs,       0x9000,   0x6000
phy_init,   data, phy,       0xf000,   0x1000
factory,    app,  factory,   0x10000,  0x1E0000
movie,      data, undefined, 0x1F0000, 0x210000
